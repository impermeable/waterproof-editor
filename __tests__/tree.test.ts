import { TreeNode } from "../src/mapping";

const type = "";
const range = {from: 0, to: 0};
const title = "";

test("nodesInRange", () => {
    // const nodes: TreeNode[] = [
    //     new TreeNode(type, range, range, title, )
    // ]
    // const tree = new Tree(nodes);
    // expect(tree.nodes.length).toBe(3);
    // expect(tree.nodes[0].from).toBe(1);
    // expect(tree.nodes[1].from).toBe(2);
    // expect(tree.nodes[2].from).toBe(3);
});